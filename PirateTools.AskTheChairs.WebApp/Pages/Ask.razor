@page "/Ask"
<PageTitle>Ask your BuVo</PageTitle>

<h1 class="mb-4 mt-4">Frage stellen</h1>

<div class="mb-4">
    <label class="form-label">Titel (Max 255 Zeichen)</label>
    <input class="form-control" @bind=Title maxlength="255" />
</div>
<div class="mb-4">
    <label class="form-label">Inhalt (Max 4096 Zeichen)</label>
    <textarea class="form-control" style="height: 15em;" @bind=Content maxlength="4096"></textarea>
</div>
<div class="mb-4">
    <label class="form-label">EMail (Optional)</label>
    <input type="email" class="form-control" @bind=EMail maxlength="1024" />
    <small>
        Du kannst eine Frage stellen ohne deine E-Mail an zu geben, dadurch ist die Frage Anonym gestellt
        und wird in der nächsten BuVo Sitzung beantwortet.<br />
        Wenn du deine E-Mail an gibst kann der BuVo deine Frage auch schon vorher beantworten, du bist dann
        aber nicht mehr Anonym!<br />
        In keinem Fall wird deine E-Mail öffentlich angezeigt. Nur der BuVo kann sie einsehen um dir eine
        Antwort zu schicken.
    </small>
</div>

@if (!string.IsNullOrEmpty(SubmitResult)) {
    <div class="mb-4">
        @SubmitResult
    </div>
}

<div class="d-flex justify-content-between">
    <a href="/" class="btn btn-warning">Abbrechen</a>
    <button class="btn btn-success" disabled="@(!CanSubmit)" @onclick=AskQuestion>Frage stellen</button>
</div>